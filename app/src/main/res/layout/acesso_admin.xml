<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp">

    <EditText
        android:id="@+id/useradmin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPersonName"
        android:hint="Usuário Admin" />

    <EditText
        android:id="@+id/senhaadmin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPassword"
        android:layout_below="@+id/useradmin"
        android:hint="senha"
        android:layout_alignParentStart="true" />
    <Button
        android:id="@+id/confirmarAdm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CONFIRMAR"
        android:textStyle="bold"
        android:layout_below="@+id/senhaadmin" />
    <Button
        android:id="@+id/cancelarAdm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CANCELAR"
        android:textStyle="bold"
        android:layout_alignBaseline="@+id/confirmarAdm"
        android:layout_alignBottom="@+id/confirmarAdm"
        android:layout_toEndOf="@+id/confirmarAdm" />
</RelativeLayout>